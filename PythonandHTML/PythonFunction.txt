print(内容)
    输出数据
str(内容)
    将其他数据转化为字符串
int(内容)
    转化有数字组成的字符串或布尔值为整数
    布尔值True为1，False为0
type(内容)
    检测输入内容类型，输出为type类数据
len(内容)
    检测字符串的字符数、列表元素数、元组元素数、字典键数（重复不计）。
    简单来说就是检测长度。
range(整数最小值, 整数最大值)
    在指定范围内找出所有整数（算头不算尾）
    最小值不写是从0算起
input(字符串提示内容（可省略）)
    在输出区输入（字符串）内容
